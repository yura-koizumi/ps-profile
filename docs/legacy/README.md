# legacy

現行実装に置き換えられた旧版を、記録として残しています。

| ファイル | 状態 |
|---|---|
| `Manage-PxProxy.ps1` / `.md` | v1 の Px プロキシ管理実装。現行は `modules/PSProfile/Proxy.ps1`（px-on / px-off / px-state）が置き換え済み |

`Manage-PxProxy` は git 管理下に入る前の実装で、どのリポジトリの履歴にも残っていませんでした。
SharePoint の `成果物アーカイブ/03_コード・ツール/0615_開発コード一式` にのみ存在したため、
2026-09-01 にここへ収容しました。動作させる用途はありません。
